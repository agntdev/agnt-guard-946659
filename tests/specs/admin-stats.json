[
  {
    "name": "View Stats shows the empty-state summary",
    "steps": [
      {
        "send": { "text": "/mod", "chatId": 150, "userId": 1 },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Moderation panel. Pick an action, then choose a member to apply it to." }
          }
        ]
      },
      {
        "send": { "callback": "admin:stats", "chatId": 150, "userId": 1 },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "📊 Moderation stats (last 200 actions)\n\nJoins: 0\nVerifications: 0\nWarns: 0\nMutes: 0\nKicks: 0\nBans: 0\nTrusted: 0\n\nSettings changes: 0\nBlocked settings attempts: 0\n\nNo moderation activity yet — actions will appear here."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "Stats explain when a notification target is not configured",
    "steps": [
      {
        "send": { "text": "/mod", "chatId": 410, "userId": 1 },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Moderation panel. Pick an action, then choose a member to apply it to." }
          }
        ]
      },
      {
        "send": { "callback": "admin:stats", "chatId": 410, "userId": 1 },
        "expect": [{ "method": "editMessageText" }]
      },
      {
        "send": { "callback": "admin:stats:send", "chatId": 410, "userId": 1 },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No notification target set. Open Settings to add one first." }
          }
        ]
      }
    ]
  }
]
