[
  {
    "name": "Only the configured bot owner can open settings",
    "steps": [
      {
        "send": { "callback": "config:panel", "chatId": 400, "userId": 1 },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Only the bot owner may edit bot settings." }
          }
        ]
      }
    ]
  }
  ,
  {
    "name": "Only the deployment owner can open the global activity reset",
    "steps": [
      {
        "send": { "callback": "config:privacy", "chatId": 401, "userId": 1 },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Only the deployment owner can clear activity data across groups." } }
        ]
      }
    ]
  }
]
